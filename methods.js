// easy method
const len = (data) => {
  return data.length;
};
//easy Math
const floor = (number) => {
  return Math.floor(number);
};
const ceil = (number) => {
  return Math.ceil(number);
};
const round = (number) => {
  Math.round(number);
};
